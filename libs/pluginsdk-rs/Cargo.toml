[package]
name = "notbbg-pluginsdk"
version = "0.1.0"
edition = "2021"
description = "Notbbg plugin SDK — Rust port of libs/pluginsdk."
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
path = "src/lib.rs"
